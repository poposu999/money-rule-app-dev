name: Ver.47.94 update

on:
  push:
    branches: [develop]
    paths:
      - '.github/workflows/ver47-94.yml'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: develop
          fetch-depth: 0
      - name: Apply update
        run: python3 .ver47_94_update.py
      - name: Validate JavaScript and diff
        run: |
          node --check app.js
          node --check sw.js
          git diff --check
      - name: Validate calculator cleanup
        run: |
          ! grep -R -nE 'calculateAmountExpression|bindAmountCalculator|1200\+300|10000\+2000|5000\+1000' README.md app.js index.html manifest.json style.css sw.js
          ! grep -R -n '47\.93' README.md app.js index.html manifest.json style.css sw.js
          grep -n 'const VERSION="47.94"' app.js
          grep -n 'money-rule-app-v47.94' sw.js
      - name: Remove temporary automation files
        run: |
          rm -f .ver47_94_update.py .github/workflows/ver47-94.yml
          rmdir .github/workflows 2>/dev/null || true
          rmdir .github 2>/dev/null || true
      - name: Validate final tree
        run: |
          python3 - <<'PY'
          from pathlib import Path
          expected={'README.md','app.js','index.html','manifest.json','style.css','sw.js'}
          actual={p.name for p in Path('.').iterdir() if p.name != '.git'}
          if actual != expected:
              raise SystemExit(f'final top-level tree mismatch: {sorted(actual)}')
          PY
          node --check app.js
          node --check sw.js
          git diff --check
      - name: Commit result
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'ver.47.94: remove amount expression calculator'
          git push origin develop

# retry after validation fix
