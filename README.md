# 家計簿 ver.49.12（検証版）

## ver.49.12の変更点

- 予定支出・固定費の編集画面では、金額・支払い状態・並び順を保存時にまとめて確定し、キャンセル・×・背景タップでは破棄するよう統一しました。
- 編集画面から支払済みにする場合、編集中の金額・カテゴリ・メモ・日付を実支出へ反映します。
- 固定費の支払済み取消は保存時に確認し、承認後に紐づく実支出を削除します。
- 固定費一覧の状態バッジを表示専用にしました。一覧の「支出に確定」は従来どおり直接操作できます。
