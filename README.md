# 家計簿アプリ

## ver.48.02
- 検証環境の保存先を `moneyRuleDevAppV2` / `moneyRuleDevSectionPrefs` / `moneyRuleDevStatPrefs` に分離
- 初回起動時のみ本番側の既存データを検証環境へコピーし、以後は本番と自動同期しない仕様に変更
- 検証環境のバックアップ書き出し・読み込みを検証用データだけに限定し、本番データへ書き込まないよう変更
- 旧形式バックアップを検証環境へ読み込む場合も、本番用キーではなく検証用キーへ復元する仕様に変更
- 検証環境の Service Worker を永続キャッシュ非使用のネットワーク取得方式へ変更し、本番側のキャッシュと干渉しにくい構成へ変更
- index.html / app.js / アプリ下部のバージョン表記を ver.48.02 に統一
